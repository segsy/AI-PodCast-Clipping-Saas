[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Omit .next folder from secrets scanning since it's generated build output
  # that may contain embedded environment variable references
  SECRETS_SCAN_OMIT_PATHS = ".next/**"
